# Placeholders

Primitive stand-ins for the paid assets listed in `docs/ASSETS.md`. Scenes and prefabs reference these so the
project builds and runs when `Assets/ThirdParty/` is absent (fresh clone, CI).
